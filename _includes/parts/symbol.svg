<svg {% if include.title %}title="{{ include.title }}"{% endif %} {% if include.role %}role="{{ include.role }}"{% endif %}>
  <use xlink:href="{{ include.id | prepend: '/assets/sprite.svg#' | relative_url }}" />
</svg>
